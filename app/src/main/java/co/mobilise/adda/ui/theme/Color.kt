package co.mobilise.adda.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Adda design-system palette (exact hackathon spec).
 * Use these everywhere instead of hard-coding hex values.
 */
val AddaBackground = Color(0xFF0D0D0F) // app background
val AddaSurface = Color(0xFF1A1A20)    // cards / sheets
val AddaOutline = Color(0xFF232330)    // borders / dividers
val AddaPrimary = Color(0xFFF5A623)    // amber — primary actions
val AddaSecondary = Color(0xFFFFC15E)  // lighter amber — accents
val AddaText = Color(0xFFFFFFFF)       // primary text
val AddaMuted = Color(0xFF9A9AA8)      // secondary / muted text
val AddaSuccess = Color(0xFF39D98A)    // connected / done
val AddaError = Color(0xFFFF6B6B)      // errors / disconnect

// Tonal helpers
val AddaPrimaryDim = Color(0xFF3A2E14) // amber tint for subtle fills
val AddaOnPrimary = Color(0xFF1A1200)  // text on amber buttons
